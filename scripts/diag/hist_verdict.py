#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""⛔ 已停用（tombstone）—— 请改用 `scripts/diag/hist32_chain.py`。

本文件是 hist32（时序编码器）判读的**第一版**实现，2026-09-20 审计时发现
它同时带着两个 bug，且**都不会报错**，于是退役而不是修第二份拷贝。

### 为什么退役而不是修

`hist32_chain.py` 的文件头写得很清楚：它之所以存在，就是因为
`hist_chain.py` 依赖 `/tmp/hist_verdict.py` 而那个文件**只在本地** ⟹ 哑火。
它同时明确写了自己**自带判读**、不依赖第二个文件。

而本次审计发现的 bug，**根因正是"同一套判读逻辑存在两份"**：
两份从同一个模板抄出来，其中一份改了、另一份没改，就漂移了。
`stats_crit.py` 的文件头也立了同一条规矩：「**唯一来源**，不许在别处再写一份」。

所以正解不是把第二份也改对（那只会在下一次改动时重新漂移），
而是**让第二份上岗失败**。判读的唯一入口是 `hist32_chain.py`。

### 两个 bug（留档，供回溯）

**(1) 臂名恒不命中 ⟹ 哑火。** 原值 `ARM = "hist"`，而 `read()` 拼的是
    `"%s/%s_s%d" % (R, ARM, s)` ⟹ 得到 `outputs/hist_s42`。
    实测该目录**从来不存在**（真实目录是 `hist32_s42`）⟹ `read()` 恒返回 `[]`
    ⟹ 本脚本**一辈子只会打印「数据不足，无法判读」**。
    危险之处在于：那看起来像「还没跑完」，不像 bug ⟹ 会**无限期地**
    留着「判读过了/在等」的假信心。

**(2) df 的静默宽松回退 ⟹ 假显著。** 原写法
        `crit = CRIT3 if df == 2 else (2.776 if df == 4 else 2.145)`
    只覆盖 df=2 与 df=4，**其余一律落到 2.145**——那是 **df=14** 的值。
    而本实验 s44 长期被 `MAX_HIST=2` 挡着（两条 hist 并发就能把 7 run
    推到 252 GB）⟹ 判读时最常见的恰恰是 **n=2（df=1）**，真值 **12.706**。
    回退给的 2.145 **宽了 6 倍** ⟹ |t| 落在 2.145~12.706 之间即**假显著**。
    同类错误见记忆 `silent-lenient-fallback-in-thresholds`。

### 如果将来真的想要第二个意见

要的应该是**不同的方法**（例如 bootstrap 重采样、或对逐轮轨迹做置换检验），
那才提供独立的证据。把同一个 t 检验再写一遍**不构成交叉验证**——
两份拷贝共享同一个错误，只会一起错（记忆 `cross-check-must-compare-same-population`
讲的是同一件事的另一个面）。

### 原判据（已迁至 hist32_chain.py，此处仅备查）

    · Δ ≥ +0.035 ⟹ 时序信息**有用**，值得加深（多通道 / 接边）
    · |Δ| < 0.035 ⟹ n=3 分辨率下**测不出**（不是"无用"）
    · Δ ≤ −0.035 ⟹ 时序编码器**有害**
    临界值一律按 df 现取，见 `stats_crit.t_crit`（未知 df 抛错，不回退）。

    · u1 守卫：hist 与 ent01_rerun 的 u1 必须**逐位相同**（同 BC、同种子、同节点）
    · 吞吐代价：LSTM 每步对 90 节点跑一次，变慢必须算进"值不值"
"""
from __future__ import annotations

import os
import sys

# 让它**响亮地失败**，而不是打印一段看起来像"还没跑完"的空判读。
#
# 退出码 2 是刻意的：任何把"没有输出"当成"还没准备好"的调度器/看门狗，
# 都会在这里看到硬失败，而不是静默地继续等（记忆 `failed-launch-must-be-loud`、
# `wait-handles-must-have-timeout`——"坏了"与"还在跑"必须可区分）。
_TOMBSTONE = """\
⛔ hist_verdict.py 已停用（2026-09-20）。

   原因：臂名恒不命中（`hist_s42` 不存在）⟹ 只会打印「数据不足」；
         且 df 未知时静默回退到 2.145（df=14 的值）⟹ 可制造假显著。

   请改用： scripts/diag/hist32_chain.py
           （自包含、臂名正确、临界值取自唯一来源 stats_crit.py）

   本文件保留仅为留档，详细说明见文件头 docstring。
"""

sys.stdout.reconfigure(encoding="utf-8")
print(_TOMBSTONE, file=sys.stderr)
sys.exit(2)
