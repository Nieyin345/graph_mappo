# 当前状态与优化方向

> **这是什么**：一页纸的**现状快照与下一步**——现在在哪、还开着哪些方向、
> 每个缺口为什么重要。
> **怎么用**：找方向看这份；看**演进历史**读
> [`模型优化历程与下一步实验.md`](模型优化历程与下一步实验.md)；
> 查**原始数字与推导**读 [`训练诊断记录.md`](训练诊断记录.md)（只增不删）；
> 查**判据**读 [`测试规范.md`](测试规范.md)。
> 本文件只放结论 + 出处，不复制推导。
> 快照日期：**2026-09-24**。数字过期以实验日志与脚本输出为准。

---

## 0. 一句话现状

**v3（pair-path 注意力主导）修复评分 bug 并做需求隔离后，是当前最强的可复现
配置（0.7565 / 0.8545 双窗口）；但它与专家锚 0.7674 的同协议配对比较还没做，
「RL 是否已超专家」仍然没有答案。**

当前主线产物：`qkd_rl/model_zoo/{v2,v3}`（统一模型目录）+
`scripts/experiment.py`（统一入口）+ 需求隔离配置
`model_zoo/v3/config_demand_isolated.yaml`（显式开关，尚未转正为默认）。

---

## 1. 现在的参考点（哪个数字能和哪个比）

> **读数纪律**：以下三组数字**互相之间不能直接比**——窗口、种子、预算不同。
> 同组内才可比。完整口径见 [`模型优化历程与下一步实验.md`](模型优化历程与下一步实验.md)「现行可复现实验协议」。

### A 组 · v3 主线（8 天专家轨迹 BC + 10 次 PPO，训练种子 42/43/44，留出 330–365 / 100–114）

| 配置 | 第 10 次更新平均 | 另一窗口 300–329 / 200–214 |
|---|---|---|
| v2 | 0.7281 | — |
| v3（默认需求传播） | 0.7376 | 0.8244 |
| **v3 + 需求隔离（当前最优）** | **0.7565** | **0.8545** |

需求隔离是**当前最有价值的结构改动**（两窗口 × 三种子同向，秘钥比同步下降），
但尚未在不同流量强度/拓扑下复验，暂以显式配置存在、不改默认。

### B 组 · 修复前的旧读数（只用于历史对照，勿与 A 组排名）

| 读数 | 值 | 说明 |
|---|---|---|
| 专家锚（现环境，删 Stockholm 后） | **0.7674** | 节点无关（纯启发式），仍有效 |
| 旧 v3（bug 修复前，20 次 PPO） | 0.7211 | 评分 bug 压低 |
| 旧 v3 修复评分、同权重重评 | 0.7528 | 找回 3.2 点；权重坏在 BC |
| 留存 BC checkpoint 重评 | **0.7490** | **更正**：曾误报 0.7971（评估脚本窗口覆盖 bug），勿再引用 |
| v2（50 次 PPO） | 0.7707 | 预算不同，不能与 A 组比 |

### C 组 · 唯一强成立的正面结果（全项目信噪比最高）

**RL 对专家省密钥 34.4%**（45/45 同向，\|t\|>6）。注意：奖励对生成量完全无感
（`generated_weight=0`），这是「无人看守的零空间漂移」——论文里必须写成
**副产品**，且可能随训练漂移。

---

## 2. 已定案 / 已否决（不要再走）

> 原始数字与推导都在 [`训练诊断记录.md`](训练诊断记录.md) 与
> [`定稿结论.md`](定稿结论.md)。这里只留一行结论。

**硬结论（有 n≥5 或 15/15 证据）**

- BC 暖启动必须保留：从零训 −12.1 点、方差 10.5×
- `demand_edge` 模式显著更差：−0.0527，p=0.0003，15/15 同向
- BC 不是越久越好：ep32 优于 ep60（12/15 下降）⟹ 起 RL 用 ep-32 快照
- 终止语义保持 `terminated`：改 `truncated` 实测 −0.0399，已回滚
- `max_weight_matching` 解码与 PPO on-policy 冲突（30–40 h/臂），不用
- 删 Stockholm 是环境修正：专家 0.6979→0.7674（15/15），旧环境臂不可比

**关闭的线索（测不出 / 前提倒塌）**

- urgency 加权等待惩罚：n=5 剂量单调是假象，n=14 效应消失（Δ=+0.0044，t=+0.87）
- 「让模型看见需求」四连败：demand_edge / v1 / v2 / DemandResidual 均测不出
- `entropy_coef` 线索：n=5「超专家」被节点混淆解释（+0.0197 硬件偏置），正式关闭
- 降 critic_lr、lr 退火、epochs 2、value_coef 1.0、minibatch 512（内存否决）等一批
- `cmax` 单点显著作废（预注册窗口 t=+0.53）——判决按预注册窗口
- 「RL 超过专家」「scratch 首个超专家」：n=15 重标定 / max-of-k 偏差推翻

**工程红线（踩过两次以上的）**

- 增益类超参微调不可测：SD(Δ)≈0.0137，测 0.5 点需 n>40
- 零初始化掐死注意力——同一 bug 已出现三次（v3 末层 / demand_residual / warmstart 零填充）
- n=5 显著性必须先怀疑选择偏差；跨节点读数不可混入配对
- 门/判据类代码必须打印输入；新通路实验必须打印梯度范数
- 训练/评测/探针一律在服务器跑；`outputs/` 只增不删

---

## 3. 下一步优化方向（按优先级）

### P0 · 主结论缺口（补上才能写论文）

| # | 缺口 | 为什么重要 | 成本 |
|---|---|---|---|
| 1 | **修正+隔离的 v3 vs 专家 0.7674 的同协议配对比较** | 当前最强配置与基线没有正面答案；这是论文的核心表 | 评测脚本现成，主要是跑 |
| 2 | **H₁ 单智能体 DRL 主对照** | 论文假设点名的对照（DQN/GAT-DRL）全仓库未实现；当前 MAPPO 架构上已是事实单智能体，但 `mutual_choice` 互选是初始化冻结不是学出来的 | 实现一个对照训练器 |
| 3 | **匹配概率契约复核** | PPO 目标已改为联合 log-prob 求和（见 commit `ad2bf03`）；若确认历史臂有系统偏差，已发结论的口径要注明 | 分析 + 一次修复臂（`train_joint_ppo_fix.yaml` 已建） |

### P1 · 结构结论补全

| # | 实验 | 状态 |
|---|---|---|
| 4 | 注意力消融链：温度实验（2 vs 1）→ 移除 cross-attention 独立消融 → 四头饱和判定 | 已规划，未开跑；基线 = 需求隔离版 |
| 5 | 需求隔离转正复验：不同流量强度 / 拓扑下不退化才并入默认 | `config_demand_isolated.yaml` 已建 |
| 6 | hist32（LSTM 历史编码器）收口——唯一没判定完的结构消融 | 待判 |
| 7 | include_stocked_edges 观测臂 | 配置已建，无日志条目 |

### 方法论纪律（比任何单个实验都重要）

- 预注册窗口 + 预注册判据，**判决只按预注册口径**（cmax 教训）
- 配对比较的两侧必须同机、同配置链、同种子协议
- 训练种子是分辨率瓶颈：加训练种子而不是验证种子
- 诊断场景只能筛「能不能学」，不能定「哪个更好」

---

## 4. 工程约束速查

| 约束 | 细节 |
|---|---|
| 本机不跑 python | hook 拦截；一切走 `ssh qkd`；服务器路径 `/opt/qkd/graph_mappo` |
| 起 run 两项必带 | `ulimit -n 65535` + `OMP/MKL_NUM_THREADS=4`（缺任一静默出事，见 CLAUDE.md） |
| 并发判据 | `MemAvailable / 30`（按跑满算），先测再算，PSS 是配置相关的比例 |
| 磁盘 | 专家轨迹很占（60 天 ≈ 2 GB），铺全量前 `df -h /opt/qkd` |
| 换节点 | 地址直接替换；长实验结果先抓回本地（`scripts/diag/fetch_results.sh`） |
| 文档互链 | 索引见 [`00_索引.md`](00_索引.md)；过期文档在 `归档/` |

---

## 5. 这份文档怎么更新

| 什么时候 | 改哪 |
|---|---|
| P0/P1 实验出结论 | §3 对应行更新状态，数字进 §1；推导进 `训练诊断记录.md` |
| 新的结构发现 | 先写 `训练诊断记录.md` 留原始数字，这里只留一行结论 + 出处 |
| 换最优配置 | §0「一句话现状」和 §1 A 组同步更新 |
| 数字过期 | 以脚本输出为准并更新快照日期 |
