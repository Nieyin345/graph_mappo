# 实验日志（用户接管版）

> **这是什么**：**评测基准与实验记录的唯一入口**。
>
> **用户 2026-09-21 指定**：
> - 「重新开一个新的日志，我来接管，不让你自己决策跑训练了」
> - 「把基线结果、统一的评测环境都写进去先，一会我们讨论一下要训练的内容」
>
> **⟹ 本文件的性质**：
> - **训练决策由用户做**，本文件只记录**已定的评测基准**与**讨论结论**
> - 我不再自行决定启动训练
> - `训练诊断记录.md`、`优化日志.md` 退为**参考**
>
> 建立于 **2026-09-21**。

---

## 第一部分：统一的评测环境（**这是基准，所有对比都必须用这一套**）

### 1.1 两个 regime 的区分（**别混**）

| | **训练 regime** | **验证 regime** |
|---|---|---|
| 天数窗口 | 0–295（`activation_window` 限制回合**起始日**） | **330–365** |
| 回合长度 | **1440 步** | **240 步** |
| 回合起始模式 | `random_day` | `random_day` |
| 种子（请求） | 训练种子 `--seed` | **100–114（固定 15 个）** |
| 出处 | `configs/train_full_rl.yaml` 的 `env:` | 同文件的 `validation:` |

**两个 regime 不可混用**：训练侧成功率 ~0.86 已近饱和（看不出差距），
差距在验证 regime。

### 1.2 验证 regime 的规范构造（**唯一入口**）

```python
# qkd_rl/evaluation/test_protocol.py
load_validation_profile(cfg_path)      # 读 validation 段；读不到就抛错，不用默认值兜底
build_validation_env_config(profile, include_baselines=..., episode_steps=240, start_mode="random_day")
```

**配置链**（`build_validation_env_config` 内部）：

```
load_default_config()
  → deep_merge(configs/env_full.yaml)          ← ★ 我们改的 exclude 在这里
  → deep_merge(configs/baselines.yaml)         （仅 include_baselines=True 时）
  → rate_provider.provider = "h5"
  → env.episode_start_mode = "random_day"
  → env.episode_steps = 240
  → activation_window = [330, 365]、window_days = 35
  → scenario.time_limit.days = 365 + ceil(240/1440) = 366
```

**★ 关键**：`env_full.yaml` 被套在这一层——所以**专家、基线、训练器的
`evaluate_validation` 读的是同一份配置**（含 `exclude: [Stockholm]`）。

### 1.3 三个评测入口（**已对齐，写新脚本照抄任一处**）

| 入口 | 文件 | 说明 |
|---|---|---|
| 专家 | `scripts/eval/eval_expert.py --seeds 100-114` | 用 `PathScoreGreedy(weights=(1,10,1,0.5,0.2), phased=True, principles=False, router=ServeProbe(env))` |
| 全部基线 | `scripts/baselines/run_baselines.py` | 读 `configs/baselines.yaml` 的开关 |
| 训练器内置 | `mappo_trainer.evaluate_validation()` | 用 `validation.request_seeds = [100..114]`、`episodes: 15` |

**★ 三处的种子来源**（已核实一致）：

```yaml
# configs/train_full_rl.yaml
validation:
  window: {start_day: 330, end_day: 365}
  start_mode: random_day
  episode_steps: 240
  episodes: 15
  request_seeds: [100, 101, ..., 114]
```

⚠ **`configs/global.yaml` 里的 `validation.request_seeds: [7,8,9,10,11]` 和
`episodes: 5` 是旧的**，训练与专家脚本都**不读它**（被 `train_full_rl.yaml`
覆盖，或由 `--seeds` 覆盖）。**别照 global.yaml 写种子。**

### 1.4 评测调用路径的 token 级细节（**可比性的前提**）

RL 策略 step 环境时必须传：

```python
env.step(actions, scores,
         edge_scores=st.edge_scores,
         expected_matched_edges=(None if resolver_mode in DETERMINISTIC_RESOLVER_MODES
                                 else list(st.matched_edges or [])))
```

专家是 `env.step(actions, scores)`（不传 edge_scores）。
`eval_expert.py`、`Evaluator._act`、`trainer.evaluate_validation` **三处已对齐**。

### 1.5 `start_seed` 的用法（**容易错**）

```python
env.reset(seed=seed, start_seed=start_seed_base + seed)
```

- `seed` = **请求种子**（100–114）
- `start_seed_base` = `validation.start_seed`（默认 **0**）
- 两者相加是环境起日种子 ⟹ **专家脚本与训练器用同一加成**

---

## 第二部分：基线结果（**本轮新测，2026-09-21**）

### 2.1 环境变更说明（**历史臂不可比的原因**）

2026-09-21 从图里删掉了 Stockholm（拓扑孤岛，度为 0）：

```yaml
# configs/env_full.yaml
scenario:
  active_nodes:
    exclude: [Stockholm]
```

| | 变更前 | 变更后 |
|---|---|---|
| 节点数 | 90 | **88** |
| 候选边 | 1978 | **1948** |
| GS 数 | 30 | **29** |
| GS 对 | 435 | **406** |
| 不可达对 | **29**（全含 Stockholm） | **0** |

> **详细论证见** [`优化日志.md`](优化日志.md) 第一节（可达性怎么算的、
> 为什么删节点而不是改 registry、预检三道门、机制对账）。

⚠ **所有 2026-09-21 之前跑的训练臂（`ent01_rerun_s42..54` 等）都在旧环境上**，
与下表**不可直接比较**。

### 2.2 基线总表（**新环境**，验证 regime，种子 100–114，240 步，n=15）

| 策略 | SR 均值 | SD | 说明 |
|---|---|---|---|
| `greedy_qkp` | 0.121392 | 0.1517 | 低库存优先 |
| `random` | 0.130057 | 0.0251 | 随机 |
| `greedy_matching` | 0.189334 | 0.1994 | 匹配得分 |
| `greedy_demand` | 0.296848 | 0.2425 | 需求优先 |
| `greedy_relay_diffusion_v3` | 0.397530 | 0.1534 | 中继扩散 v3 |
| `greedy_rate` | 0.490491 | 0.1077 | 速率优先 |
| `greedy_relay` | 0.492071 | 0.1061 | 中继优先（**强基线**） |
| **`expert`（`path_score_greedy_phased`）** | **0.767435** | 0.1317 | **★ 对照锚** |

**产出文件**：
- `outputs/eval/baseline_paired_exclude.json`（本地备份 `server_results/`）
- `outputs/eval/expert_stockholm_removed.json`（本地备份 `server_results/`）

### 2.3 基线因删节点而上升（**配对，同 15 种子**）

| 策略 | 不删 | 已删 | Δ | 同向 |
|---|---|---|---|---|
| random | 0.126321 | 0.130057 | +0.0037 | 7/15 |
| greedy_qkp | 0.107498 | 0.121392 | +0.0139 | 8/15 |
| greedy_relay_diffusion_v3 | 0.378926 | 0.397530 | +0.0186 | 10/15 |
| greedy_matching | 0.169339 | 0.189334 | +0.0200 | 12/15 |
| greedy_demand | 0.256426 | 0.296848 | +0.0404 | 8/15 |
| greedy_rate | 0.445643 | 0.490491 | +0.0448 | 12/15 |
| greedy_relay | 0.446180 | 0.492071 | +0.0459 | 13/15 |
| **expert** | 0.697922 | **0.767435** | **+0.0695** | **15/15** |

**★ 全部 8 个同向上升**，但幅度差 **12 倍**（random +0.4 点 vs 专家 +6.95 点）
—— 弱策略本来就没在服务，少掉不可达需求对它们影响小。

**机制对账（不是分母效应）**：

```
arrived（分母）= 18,078,315 → 18,126,378   +0.27%   ← 几乎没变
served （分子）= 12,879,569 → 14,107,397   +9.53%   ← 在这里
```

请求总数不变（总到达率固定），但原来 6.67% 的对贡献 arrived、贡献 0 served。
删掉后同样多的请求全落在可服务的对上。

### 2.4 读数可信度（**独立复现**）

配对探针与单独跑的 `run_baselines`（同 5 个种子 100–104）**逐位一致**
（差 `0.00e+00`）⟹ 两个入口给出相同读数。

### 2.5 基线怎么用（**判据，别用错**）

| 用法 | 对 | 错 |
|---|---|---|
| 与**专家**比 | ✅ 专家 0.767435 是论文的对照锚 | — |
| RL 臂之间比 | ✅ 必须**同环境**（都删了节点） | ❌ 拿旧环境的臂比新环境 |
| 拿 `greedy_relay` 当对照 | ⚠️ 比专家弱 27 点，只能说明「比弱基线好」 | ❌ 拿它当「击败对象」 |
| 比绝对值 | ⚠️ 跨策略绝对值无意义 | ❌ 用 Δ 之外的量下结论 |
| 配对 | ✅ 必须**逐种子配对**（不配对 SE≈0.062，配对≈0.012） | ❌ 只看均值差 |

---

## 第三部分：待讨论（**训练什么，由用户定**）

以下是我认为需要讨论的点，**不是我要执行的计划**：

1. **RL 臂要在新环境重跑**——旧臂跑在含 Stockholm 的环境上，
   无法与专家 0.767435 对比。重跑要几个种子？
2. **奖励改动**（用户提的失败惩罚 + 成功率 delta）——有一处前置测量没做：
   `failed` 与 `served` 是否高度负相关？若是，加大惩罚 ≈ 加大 served 权重，**冗余**。
3. **预训练是否困住上限**（用户的假设②）——从零训对照的种子扩展。
4. **GNN 是否过重**（E1 去图臂）——n=5 时 p=0.053，差一点。

---

## 第四部分：urgency 加权等待惩罚 —— 对比训练计划（**2026-09-22 定**）

> **状态**：机制已实现并验收（四道门全过），**尚未起臂**。

### 4.1 机制一句话

每个**未被服务**的请求按**剩余寿命**加权后被罚：

```
urgency(req) = 1 − 剩余步数 / deadline_steps     （0 = 刚到，1 = 快死）
penalty      = urgency_wait_weight × Σ urgency×剩余量 / served_reference
```

逐 step 计算 ⟹ **及时**；**不规定顺序**（模型自己决定先做谁，只是救急的回报更高）。

### 4.2 为什么需要它（与现状的对照）

生效配置里三项奖励（训练 regime 实测 570 轮）：

| 项 | 每步均值 | 占 served | 对 urgency 敏感吗 |
|---|---|---|---|
| `served_weight`（服务成功） | 0.066313 | 100% | ❌ 服务 1 个 key 恒定给 5e-4，不论多急 |
| `failed_weight`（过期失败） | 0.001068 | **1.61%** | ❌ 只在过期那一刻扣，是**事后**信号 |
| **`urgency_wait`（新）** | 见 4.4 | — | ✅ **唯一的「优先救谁」方向信号** |

**★ 关键**：`served` 与 `failed` 只定奖励**水平**，不定**方向**——
服务 A 而让 B 过期，净得 `w_s − w_f`；反过来选 B 一样。
只有 urgency 项能区分「先救急的」和「先服务从容的」。

### 4.3 验收记录（起臂前必须全过）

| 门 | 判据 | 结果 |
|---|---|---|
| ① | 诊断字段 `urgency_weighted_keys` 不被 `reward_scale` 缩放 | ✓ 逐位一致 |
| ② | 手算 = env 输出（4 个抽样点） | ✓ 逐位一致 |
| ④ | `w=0` 时新项逐位为 0，且 served/failed 逐位不变 | ✓ 正交 |

**造反证抓到我自己的两个错误**（都已修）：

1. 诊断字段被 `reward_scale=0.002` 缩放（手算 280,013 vs env 510，差 549 倍）
2. **权重推导多除了一次 `served_reference`**——三项共用同一分母，做比值时约掉，
   正确值是 `w = failed_weight/14.5 ≈ 0.345`，不是 3.2e-6（差 5 个数量级）

### 4.4 剂量臂设计

**唯一变量**：`reward.urgency_wait_weight`。其余全部锁定。

| 臂名 | `urgency_wait_weight` | urgency 项/步（预检实测） | 占 served |
|---|---|---|---|
| **`u0`（对照）** | `enabled: false` | 0 | 0% |
| **`u1`（0.5×）** | 0.17 | 0.00104152 | 2.35% |
| **`u2`（1×）** | **0.35** | **0.00214431** | **4.83%** |
| **`u3`（2×）** | 0.70 | 0.00428863 | 9.66% |
| **`u4`（4×，上界）** | 1.40 | 0.00857725 | 19.32% |

> ★ 上表是**预检实测**（`probe_urgency_precheck.py`，验证 regime，专家策略，
> 40 步，种子 100）。此前一版用**训练 regime** 的 `waiting_keys` 外推，
> 得到 u2=1.98% —— 两个 regime 的积压量不同，**以预检实测为准**。
> 剂量仍是**线性**的（0.17→1.40 恰好 8 倍，占比 4.83%→19.32% 也约 4 倍，
> 因为占比里 served 项相同），所以「剂量」这个旋钮是**单调可控**的。

**`u2` 是「让等待与失败同等重要」的点**（4.83% vs failed 的 1.61%，即等待被罚得比失败更重），
是推导出来的平衡点，不是拍的。`u1/u3/u4` 用来量曲线形状。

### 4.5 每组训练的完整设置

```
# 公共（除 urgency_wait_weight 外全部相同）
--configs rl_algorithm.yaml train_full_rl.yaml train_ent01.yaml train_urgency.yaml
--checkpoint outputs/supervised_pg_phased/supervised_pg_phased_latest.pt
--num-updates 30
训练 regime : 窗口 0–295 天，回合 1440 步，seed = 训练种子
验证 regime : 窗口 330–365 天，回合 240 步，请求种子 100–114（15 个）
环境        : env_full.yaml（已删 Stockholm）
对照锚      : 专家 0.767435
```

| 组 | 臂名 | 种子 | 条数 |
|---|---|---|---|
| **U0** | `u0ctl_s{42..46}` | 42,43,44,45,46 | 5 |
| **U1** | `u1half_s{42..46}` | 同上 | 5 |
| **U2** | `u2bal_s{42..46}` | 同上 | 5 |
| **U3** | `u3two_s{42..46}` | 同上 | 5 |
| **U4** | `u4quad_s{42..46}` | 同上 | 5 |
| | | **合计** | **25 条** |

**★ `u0` 必须同批跑**：历史臂跑在含 Stockholm 的旧环境上，与本次**不可比**。

### 4.6 判据（**预注册**）

- **窗口**：`u25, u30` 的均值（与本项目历次判读一致）
- **配对**：逐种子配对（不配对 SE≈0.062，配对≈0.012）
- **n=5**，df=4，临界 **t = 2.776**
- **必须报「可检测效应」**（`t_crit × SE`）

| 结论 | 阈值 |
|---|---|
| urgency **有效** | 某个 `u_k` vs `u0`：Δ ≥ +0.020 且 5/5 同向 |
| urgency **有害** | Δ ≤ −0.020 且 5/5 同向（重要：可能伤成功率） |
| **测不出** | 所有剂量 \|Δ\| < 0.020（写有界结论） |
| **曲线形状** | 若 u1<u2>u3 ⟹ 存在最优剂量；若单调 ⟹ 剂量越大越好 |

⚠ **「有害」是真可能**：救快死的请求可能挤占本可从容服务的资源。
如果 u4（4×）显著更差，那就是**剂量过大**的证据，本身是有价值的结论。

### 4.7 并发与内存

```
每条 run 稳态 25 GiB ⟹ 上限 = (250 − 17) / 25 ≈ 9 条
25 条臂分 3 波：9 + 9 + 7
★ 判据是 MemAvailable/30，不是 CPU 核数；启动前必须算「待涨量」
```

### 4.8 结果（**全部 25 条完成 u30，2026-09-22**）

> 数据源：`outputs/*/metrics.jsonl` 的 u25 / u30 `eval_validation.mean_success_rate`。
> 脚本：`/tmp/collect_urgency.py` + `/tmp/_paired_urgency.py`

| 臂 | 种子 | u25 | u30 | 均值 |
|---|---|---|---|---|
| `u0ctl_s42` | 42 | 0.7613 | 0.7725 | 0.7669 |
| `u0ctl_s43` | 43 | 0.7708 | 0.7489 | 0.7599 |
| `u0ctl_s44` | 44 | 0.7668 | 0.7634 | 0.7651 |
| `u0ctl_s45` | 45 | 0.7481 | 0.7575 | 0.7528 |
| `u0ctl_s46` | 46 | 0.7757 | 0.7996 | 0.7876 |
| `u1half_s42` | 42 | 0.7566 | 0.7689 | 0.7628 |
| `u1half_s43` | 43 | 0.7699 | 0.7461 | 0.7580 |
| `u1half_s44` | 44 | 0.7711 | 0.7729 | 0.7720 |
| `u1half_s45` | 45 | 0.7735 | 0.7596 | 0.7665 |
| `u1half_s46` | 46 | 0.7806 | 0.7864 | 0.7835 |
| `u2bal_s42` | 42 | 0.7569 | 0.7585 | 0.7577 |
| `u2bal_s43` | 43 | 0.7705 | 0.7706 | 0.7705 |
| `u2bal_s44` | 44 | 0.7723 | 0.7797 | 0.7760 |
| `u2bal_s45` | 45 | 0.7750 | 0.7585 | 0.7668 |
| `u2bal_s46` | 46 | 0.7859 | 0.7795 | 0.7827 |
| `u3two_s42` | 42 | 0.7735 | 0.7658 | 0.7697 |
| `u3two_s43` | 43 | 0.7849 | 0.7710 | 0.7779 |
| `u3two_s44` | 44 | 0.7574 | 0.7731 | 0.7653 |
| `u3two_s45` | 45 | 0.7826 | 0.7753 | 0.7789 |
| `u3two_s46` | 46 | 0.7807 | 0.7855 | 0.7831 |
| `u4quad_s42` | 42 | 0.7779 | 0.7772 | 0.7775 |
| `u4quad_s43` | 43 | 0.7743 | 0.7668 | 0.7705 |
| `u4quad_s44` | 44 | 0.7775 | 0.7599 | 0.7687 |
| `u4quad_s45` | 45 | 0.7895 | 0.7784 | 0.7840 |
| `u4quad_s46` | 46 | 0.7840 | 0.7801 | 0.7821 |

### 4.9 配对比较（**全部完成**）

| 对比 | Δ | SD(Δ) | t | 同向 | 可检测效应 | 结论 |
|---|---|---|---|---|---|---|
| u1half vs u0ctl（0.5×） | +0.002094 | 0.007926 | +0.591 | 2/5 | 0.0098 | 测不出 |
| u2bal vs u0ctl（1×） | +0.004274 | 0.010560 | +0.905 | 3/5 | 0.0131 | 测不出 |
| u3two vs u0ctl（2×） | +0.008525 | 0.012984 | +1.468 | 4/5 | 0.0161 | 测不出 |
| u4quad vs u0ctl（4×） | +0.010086 | 0.013527 | +1.667 | 4/5 | 0.0168 | 测不出 |

**各组均值与 SD**：

| 组 | 剂量 | 均值 | SD | Δ | t | 同向 |
|---|---|---|---|---|---|---|
| `u0ctl` | 关闭 | 0.766466 | 0.013047 | — | — | — |
| `u1half` | 0.5× | 0.768560 | 0.009784 | +0.0021 | +0.59 | 2/5 |
| `u2bal` | 1× | 0.770740 | 0.009436 | +0.0043 | +0.91 | 3/5 |
| `u3two` | 2× | 0.774980 | 0.007575 | +0.0085 | +1.47 | 4/5 |
| `u4quad` | 4× | 0.776552 | 0.006785 | +0.0101 | +1.67 | 4/5 |

**★ 剂量-响应曲线（附图见下方结论）**：

```
剂量:      0.5×      1×        2×        4×
Δ:      +0.0021   +0.0043   +0.0085   +0.0101     ← 单调上升
t:        +0.59     +0.91     +1.47     +1.67     ← 单调上升
同向:      2/5       3/5       4/5       4/5       ← 改善
SD(Δ):   0.0079    0.0106    0.0130    0.0135     ← 上升（剂量引入了额外方差）
```

**⟹ 三条独立指标（Δ、t、同向数）全部单调改善。**
这不是随机噪声的形状——**但没有一个达到预注册阈值（Δ≥0.020 且 5/5）**。

### 4.10 阶段总结（**2026-09-22**）

#### 结论（按证据强度）

**1. 【测不出】urgency 加权等待惩罚在 n=5 下未达预注册判据。**
四个剂量的 \|Δ\| 全部 < 0.020，同向数最高 4/5。**按预注册规则，写"测不出"。**

**2. 【方向一致，且剂量-响应单调】**
Δ、t、同向数三个指标随剂量**单调改善**（+0.0021→+0.0101；
+0.59→+1.67；2/5→4/5）。**这是"可能有效但测不出"，不是"无效"。**

**3. 【★ 关键限制：这个实验的 n 不够】**

```
实测效应量（最大）      = 0.0101
可检测效应（n=5, df=4） = 0.0168
              ⟹ 效应量 < 可检测效应
```

按 `n ≈ (t_crit · SD(Δ) / Δ)²` 反算 `u4quad`：
`n ≈ (2.776 × 0.013527 / 0.0101)² ≈ **13.8**`
⟹ **要判定 u4quad 是否有效，需要 n ≈ 14，不是 5。**

**4. 【剂量越大越好，暂无拐点】**
4× 剂量仍在上升。**"最优剂量"未找到**——要么继续加大，要么接受"越大越好"。

#### 对后续实验的影响

| 事项 | 影响 |
|---|---|
| **是否采用 urgency** | **不能判定**。需要补种子到 n≥14 才能对 4× 剂量下结论 |
| **补种子的成本** | 14 条 × 1.5 h ≈ 21 h（可并行 9 条 ⟹ 约 5 h） |
| **奖励层其他改动** | 本机制**已实现、已验收**（四道门全过），随时可复用 |
| **注意力/路径实验** | **不受影响**（那是模型层，与本实验正交） |

#### 一个副产品：SD(Δ) 随剂量上升

```
0.5×: 0.0079   1×: 0.0106   2×: 0.0130   4×: 0.0135
```

**剂量越大，族内方差越大。** 这与 `bc-start-value-is-variance-not-performance`
那条「BC 的价值是降低方差」是同一类现象——**改动越强，训练轨迹越发散**。
**意味着：加大剂量既提高均值、也提高方差，净显著性提升有限**（t 只从 0.59 涨到 1.67）。

#### 产出文件

- `outputs/urgency_val_points.json`（服务器）
- 五个剂量配置：`configs/train_urgency_{u0ctl,u1half,u2bal,u3two,u4quad}.yaml`
- 进度检查：`/tmp/check_urgency.py`；收集：`/tmp/collect_urgency.py`；配对：`/tmp/_paired_urgency.py`

### 4.11 复现方式

机制实现：

| 文件 | 改动 |
|---|---|
| `qkd_rl/env/request.py` | `serve()` 多收 `deadline_steps`，产出 `urgency_weighted_keys` |
| `qkd_rl/env/reward.py` | `urgency_wait_penalty`，接进 `key_total` + 两处归一化 |
| `qkd_rl/env/env.py` | `step()` 把 `deadline_steps` 传下去 |
| `configs/train_urgency.yaml` | 剂量臂配置 |

验收与量级探针（服务器上跑）：

```
/tmp/probe_urgency_verify.py       # 四道门
/tmp/probe_urgency_magnitude.py    # 机制量级
```

起臂（实际用波启动器按内存排队，不一次全铺）：

```
ulimit -n 65536 && OMP_NUM_THREADS=8 setsid nohup \
  <venv>/bin/python -u scripts/train/train_graph_mappo.py \
  --configs rl_algorithm.yaml train_full_rl.yaml train_ent01.yaml train_urgency.yaml \
  --checkpoint outputs/supervised_pg_phased/supervised_pg_phased_latest.pt \
  --seed 42 --num-updates 30 --run-name u2bal_s42 > /tmp/u2bal_s42.log 2>&1 &
```

### 4.12 执行状态（**2026-09-22**）

| 波 | 臂 | 状态 |
|---|---|---|
| 1 | `u0ctl` 全 5 + `u1half` s42–45（9 条） | **✓ 完成 u30** |
| 2 | `u1half` s46 + `u2bal` 全 5 + `u3two` s42–44（9 条） | **✓ 完成 u30** |
| 3 | `u3two` s45,s46 + `u4quad` 全 5（7 条） | **在跑**（19:01 UTC 起） |

启动器（都在服务器 `/tmp/`）：

| 脚本 | 用途 |
|---|---|
| `launch_urgency_w1.sh` | 第 1 波（已执行） |
| `launch_urgency_wave.sh 2\|3` | 第 2/3 波，含**内存门**（判据 `MemAvailable/30`，地板 17 GiB） |
| `check_urgency.py` | 进度检查：完成/在跑/未启动/疑似死掉 |

**起臂时踩到的两个坑（已修，记下来免得重犯）**：

1. `--configs` 传了 **`configs/train_urgency_*.yaml`** ⟹ 脚本自己又加一次
   `configs/` ⟹ `FileNotFoundError: configs/configs/...`。**必须传裸文件名。**
2. 加了 `reward_urgency_wait` 到 `_ROLLOUT_DEBUG_DETAIL_KEYS` 却没加到
   `_ROLLOUT_DEBUG_KEYS` ⟹ `new_rollout_debug()` 建的累加器缺键 ⟹
   **第一步就 KeyError**，9 条臂全崩。已改成从两个源**派生**，不再是第三份
   手工列表。

> ★ 两条都是「启动后 90s 验证」抓到的 —— 只看 `ps` 有 9 个进程会误判为
> 「在跑」（实际是崩溃后的**孤儿 worker**，PPid=1、日志 3 分钟不动）。

---

## 第五部分：去死重 / 去冗余（**2026-09-22 排入，等前序跑完再执行**）

> **状态**：**待执行**。用户 2026-09-22：「我们现在不做任何代码修改，就只更新日志任务，
> 加一个去死重、去冗余的任务，然后找一个之前的实验，用相同的配置做对比实验」。
> **触发条件**：urgency 剂量实验（第四部分）跑完之后。

### 5.1 两处改动（都是**模型结构**，会改参数量）

#### 改动 A：删 `actor.idle_scorer`

**位置**：`qkd_rl/rl/models/graph_mappo.py:454-458`（定义）、
`:675` 与 `:1086`（前向两处）。

**为什么是死重**（两条独立证据，见记忆 `idle-scorer-is-dead-weight` /
`idle-scorer-has-no-gradient-not-no-input`）：

| 证据 | 结果 |
|---|---|
| 更新前后权重 | **逐位不动**（`torch.equal` = True，差恰好 `0.000e+00`） |
| `register_hook` 的梯度 | **一个都没触发** ⟹ 不在计算图里 |

**根因**：`padded_logits`（含 idle 分数的那个张量）**全仓库没有消费者**。
系统的「停」语义完全由 `stop_logit`（一个全局标量）承担——**两条停机制，死了一条**。

**规模**：`actor.idle_scorer.{0.weight,0.bias,2.weight,2.bias}` = 128×128 + 128 + 1×128 + 1
= **16,641 参数 = actor 参数量的 20%**。

#### 改动 B：去 Actor 打分公式的冗余

**位置**：`qkd_rl/rl/models/graph_mappo.py:667-670`

```python
pair_emb = torch.cat([node_emb[src_t], node_emb[dst_t], edge_emb_directed[pos_t]], dim=-1)
edge_scores = self.edge_scorer(pair_emb).squeeze(-1)
```

**问题**：`edge_emb_directed` 是 encoder 的输出，而 encoder 的每一层都做
（`graph_mappo.py:313` `_edge_message`）：

```python
msg = edge_mlp(cat([edge_emb, new_node[src], new_node[dst]]))
new_edge = edge_norm(msg + edge_emb)
```

⟹ **边嵌入里已经含有它两个端点的节点信息**（还经过 3 层累积）。
Actor 又把 `node_emb[src]`、`node_emb[dst]` 拼了一遍：**同一份信息进了两次。**

⚠ **这条不是「确定的 bug」**，是**冗余**。MLP 可以学会忽略重复项，
所以效果未知——**必须用实验回答，不能靠读代码断言**。

### 5.2 对比实验设计

**原则：用同一套配置，只改这两处，逐种子配对。**

| 项 | 内容 |
|---|---|
| **对照臂** | 已存在的 `u0ctl` 组（urgency 关闭 = 当前基线配置），5 种子 |
| **实验臂** | `clean_s{42..46}`，**除这两处外完全相同** |
| **唯一变量** | `idle_scorer` 删除 + Actor 输入去掉重复端点 |
| **种子** | 42,43,44,45,46（n=5） |
| **窗口** | u25, u30 均值 |
| **配对** | 逐种子（不配对 SE≈0.062，配对≈0.012） |
| **临界值** | n=5，df=4，**t = 2.776** |

⚠ **对照必须同批**：`u0ctl` 会由第四部分的实验同时产出，**与新环境、同种子**，
所以可直接配对，不需要重跑。

### 5.3 ★ 起臂前必须过的预检（**这次比以往更严**）

这两处改动**会改模型结构**，所以过去那套「差异字段 + 维度 + BC 权重」要收紧：

| 门 | 判据 | 为什么 |
|---|---|---|
| ① 差异字段 | 恰好是 `idle_scorer`（删除）+ `edge_scorer`（第一层输入 384→128） | 证明单变量 |
| ② **维度** | `node_dim` / `edge_dim` **不变** | 保证 BC 起点可用 |
| ③ **BC 权重加载** | `idle_scorer` 的 4 个键会出现在 **`unexpected`（多余）** 里，**这是预期的**；`missing`（缺失）必须为 **0** | 见 5.4 |
| ④ **`edge_scorer` 的形状** | `edge_scorer.0.weight: (128, 384) → (128, 128)` ⟹ **变窄，补零救不回** ⟹ 这层**会进 `dropped`** | ★ 必须先确认丢掉它不致命 |
| ⑤ **造反证** | 跑 1 轮训练，确认能正常出 `update` 行、无异常 | 防「配了但没接线」 |

### 5.4 ★★ 一个必须先说清的妥协：`edge_scorer` 会丢暖启动

**这是本次改动的真实代价，不是没有。**

```
edge_scorer 输入 = [起点节点(128), 终点节点(128), 边嵌入(128)] = 384 维
去掉重复端点后 = [边嵌入(128)]                                    = 128 维
```

**变窄 ⟹ `_expand_last_dim` 救不回**（那个函数只能加宽）⟹ `edge_scorer.*` 会进
**`dropped`**，即**该层回到随机初始化**。

同时注意 `load_checkpoint` 里还有一条（`mappo_trainer.py:1690`）：

```python
if data.config.get("reward") != self.config.get("reward"):
    self.model.critic.value_head.apply(_reset_module)   # critic 价值头本来就重置
```

**所以本次改动会丢两层**：`edge_scorer`（因为变窄）+ 本来就会重置的 `critic.value_head`。
**共享 encoder 与其余 actor 部分仍完整保留。**

⚠ **这降低了可检测效应**（暖启动损失 ⟹ 族内方差上升）。起臂前**必须**用探针量出
新族的 `Δ_s` SD，据此报「可检测效应」，否则会重蹈 `demand_edge` 那种「测不出却以为是无效」。

### 5.5 判据（预注册）

| 结果 | 阈值 |
|---|---|
| **清理有效** | Δ ≥ +0.020 且 5/5 同向 ⟹ 死重/冗余确实在损害性能 |
| **清理有害** | Δ ≤ −0.020 且 5/5 同向 ⟹ ★ 说明「冗余」其实在起作用（容量/正则） |
| **测不出** | \|Δ\| < 0.020 ⟹ 写有界结论：**影响 < X 点，但换来了更小的模型** |

**★ 「测不出」也是有用结果**：若清理后性能不变，那就是「参数少了 20%+ 而性能不掉」，
对论文的**模型简洁性**是一个正面证据。**这种情况要额外报参数量与推理时间的前后对比。**

### 5.6 结果（**待填**）

| 臂 | 种子 | u25 | u30 | 均值 | 备注 |
|---|---|---|---|---|---|
| `u0ctl_s42` | 42 | | | | 对照（第四部分产出） |
| `u0ctl_s43` | 43 | | | | |
| `u0ctl_s44` | 44 | | | | |
| `u0ctl_s45` | 45 | | | | |
| `u0ctl_s46` | 46 | | | | |
| `clean_s42` | 42 | | | | |
| `clean_s43` | 43 | | | | |
| `clean_s44` | 44 | | | | |
| `clean_s45` | 45 | | | | |
| `clean_s46` | 46 | | | | |

**配对比较（待填）**：

| 对比 | Δ | SD(Δ) | t | 同向 | 可检测效应 | 结论 |
|---|---|---|---|---|---|---|
| clean vs u0ctl | | | | | | |

**参数量对比（待填）**：

**★ 实测（2026-09-22，服务器上构造模型数的）**：

| | 改前 | 改后 | 差值 |
|---|---|---|---|
| **总参数** | **1,053,188** | 1,003,779 | **−49,409（−4.69%）** |
| `actor.idle_scorer` | 16,641 | 0 | −16,641 |
| `actor.edge_scorer` | 65,921 | 33,153 | **−32,768** |

`edge_scorer = build_mlp(384, [128,128], 1)`：第一层 `384→128` 变 `128→128`
（−32,768），后两层不变。

**参考量级**：`encoder.layers` 840,960（79.8%）、`critic.value_head` 66,305、
`encoder.*_proj` 63,360。

### 5.7 与已有结论的关系

- 记忆 `idle-scorer-is-dead-weight` 与 `idle-scorer-has-no-gradient-not-no-input`：
  两条独立证据都指向「死重」，**本实验把「死重」升级为「删掉有没有代价」的实测**。
- ⚠ **`scripts/diag/probe_idle_del_risk.py` 的结论已过期**：它说「checkpoint 里存着
  `actor.idle_scorer.*` 的键，而 `load_state_dict` 没有 `strict=False` ⟹ 删模块会抛异常」。
  **实测（2026-09-22）该链路已被取代**：`mappo_trainer.py:1666` 先调
  `_upgrade_state_dict_for_model`，它把「模型里不存在」的键收集进 `unexpected` 并**忽略**
  （`:1685` 打印「checkpoint 多余 N 个键，已忽略」），再 `load_state_dict(state)`。
  ⟹ **删模块是安全的**，只是会打印一行「多余 4 个键」。
- **同族**：本仓库的「存在但没接线」（`history_encoder` 37,760 参数、
  `success_delta` 权重 50）——本实验是针对这一类失效的**第一次实测**。

### 5.8 执行顺序

```
① 等第四部分（urgency 剂量）跑完 —— u0ctl 组即本实验的对照
② 改代码（两处）+ 跑 5.3 的五道预检
③ 起 clean_s{42..46} 五条
④ 收结果，填 5.6 两张表
⑤ 更新本文件与记忆
```

⚠ 本实验**必须在 urgency 实验全部结束后**再改代码——否则正在跑的臂会读到新代码
（记忆 `chain-must-not-be-resynced`）。

---

## 第六部分：需求→物理边的可学习注入（**2026-09-22 设计，待第五部分之后执行**）

> **状态**：**设计完成，未实现**。
> **触发条件**：第五部分（去死重/去冗余）跑完之后。
> **用户 2026-09-22 设计意图**（原话）：
> 「需求边想象是一个**源**……单向的去扩散到物理边……主要的物理边的选择去满足
> 需求边的请求……**不要用标量，要用向量去扩散**，**不要用经验公式**，
> 如果扩散要像网络一样**可以学习**」
> 「**以需求边为单位去注入**，而不是节点注入……**ab 作为一个整体去注入**，
> ab 和 ba 都是一样的」
> 「**bfs 只是用来筛选的**，筛选出来的边来进行注意力，就是一个需求边对多个
> 物理边的注意力」

### 6.1 现状的两个缺陷（用户指出的）

**缺陷 1：BFS 那个标量是「求和」，AB 和 BC 分不清**

`relay_importance` 对经过的每条请求做**加权求和**，得到**每条物理边一个标量**。
一条物理边如果同时被 AB 和 BC 两条需求覆盖，它只知道自己被「总共需要多少」，
**不知道是 AB 还是 BC**。

**缺陷 2：节点注入也是求和**

```python
# graph_mappo.py:277
aggregated = aggregated + self._node_message(demand_mlp, node_emb, src_d, dst_d, demand_emb)
#                          ↑ index_add_ 到节点，AB 和 BC 在共享节点处被求和
```

**两套现有机制都有同一个缺陷。** 用户的设计是**第一个能区分它们的机制**。

### 6.2 机制：需求边当 Query，在其 BFS 路径上做注意力

```
① BFS 筛选（拓扑事实，不需要学）
   对每条需求边 (src, dst)，筛出所有满足
       dist(src,·) + dist(dst,·) + 1 ≤ max_path_links(=8)
   的物理边 ⟹ 候选对 (需求边 i, 物理边 j)。实测每步 100~460 对。

② 注意力（权重全部可学习，无经验公式）
   Q = 需求边 i 的嵌入              ← ★ 需求边当 Q
   K = 物理边 j 的嵌入
   V = 物理边 j 的嵌入
   α_ij = softmax_j( <Q_i, K_j> / √H )    ← ★ softmax 在**每条需求自己的候选集**内
   ctx_i = Σ_j α_ij · V_j                  ← 这条需求看到的「整条通道状况」

③ Scatter 回写（把需求的判断写回它路径上的边）
   phys_emb[j] += W_out · ( α_ij · ctx_i )   ← 按注意力权重加权回写
```

**为什么需求边当 Q 是对的**（用户 2026-09-22 指正，我先前讲反了）：

> **一条物理边看不见整条路径，但一条需求边看得见。**
> 而「服务需要连成通道」是**需求的属性**，不是链路的属性。

★ 而且 `α_ij` 天然能学出「**这条路径上哪一跳最关键**」——
这正是 `v2_bottleneck` 手工表达的那个量（`argmin(hop_levels)`）。
让网络自己学，比写死公式好。

### 6.3 实测开销（2026-09-22，`probe_bfs_pairs.py`）

| step | 需求边 | 候选对 | 每条需求看多少物理边（中位/最大） | 零路径的需求 |
|---|---|---|---|---|
| 10 | 7 | 113 | 0 / 113 | 6/7 |
| 30 | 9 | 326 | 41 / 79 | 3/9 |
| 40 | 6 | 458 | 85 / 163 | 1/6 |

**候选对每步 100~460 个 ⟹ 注意力开销可忽略**（几百个点积）。

⚠ **但分布极不均**：中位数是 0，少数需求覆盖上百条边。
**⟹ 必须有「零候选时输出 0（不是 NaN）」的处理**，且要造反证。

### 6.4 ★ BC 暖启动：**能用，但取决于怎么改**

| 改法 | BC 可用？ | 可检测效应 |
|---|---|---|
| **只加**新通路 + **零初始化** + 配置开关默认关 | ✅ **完整保留** | **0.008** |
| 删掉 BFS 标量特征（`edge_dim` 44→43） | ❌ 变窄，补零救不回 | 0.17（测不出） |

**⟹ 只加不删。** 理由：模型只**新增**参数（不改任何现有张量形状）⟹
`_upgrade_state_dict_for_model` 把新键收进 `missing`（用当前初始化值），
旧键**逐位照搬** ⟹ BC 权重完整可用。

**★ 零初始化是硬要求**：

```python
nn.init.zeros_(self.dem_out[-1].weight)
nn.init.zeros_(self.dem_out[-1].bias)
```

⟹ 第 0 步 `W_out · (...) = 0` ⟹ **前向与现在逐位等价**
⟹ 真正干净的单变量对照，且 BC 暖启动无损。

### 6.5 ★★ 必须带的检查：新通路的**梯度范数**

**为什么不能省**：本项目训练已饱和（`kl≈0.001`，clip 从未激活），
actor 可能被 advantage 塌缩冻住 ⟹ **新通路可能拿不到梯度 ⟹ 停在零 ⟹
前向永远等于基线**。

**症状伪装**：「实现了但学不动」与「正确实现但确实没用」**长得一模一样**。
本仓库栽过多次（`idle_scorer`、`history_encoder`）。

```
梯度非零 ⟹ 它在参与 ⟹ 结果可解读
梯度恒零 ⟹ 停在零是"学不动"不是"没用" ⟹ 结论完全不同
```

**实现**：训练中打印新参数（`dem_q/dem_k/dem_v/dem_out`）的 `grad.norm()`，
逐轮记进 `metrics.jsonl`。

### 6.6 实验设计

**唯一变量**：`model.encoder.demand_injection: false → true`

| 组 | 臂名 | 种子 | 备注 |
|---|---|---|---|
| **对照** | `u0ctl_s42..46` | 42–46 | **第四部分已产出**，同环境同种子，直接配对 |
| **实验** | `dminj_s42..46` | 42–46 | 唯一差异是那个开关 |

| 项 | 值 |
|---|---|
| 窗口 | u25, u30 均值 |
| 配对 | 逐种子（不配对 SE≈0.062，配对≈0.012） |
| 临界值 | n=5，df=4，**t = 2.776** |
| 可检测效应 | 待量（若 5.4 的暖启动无损，应 ≈0.008~0.010） |

⚠ **对照不用重跑**——`u0ctl` 与实验臂**同环境、同种子、同批**，是干净对照。

### 6.7 判据（预注册）

| 结果 | 阈值 | 含义 |
|---|---|---|
| **有效** | Δ ≥ +0.020 且 5/5 同向 | 可学习注入优于手工 BFS 标量 |
| **有害** | Δ ≤ −0.020 且 5/5 同向 | 冗余通路损害了训练 |
| **测不出** | \|Δ\| < 0.020 | 写有界结论 |
| **★ 梯度检查** | 梯度恒零 | **结果不可读**，先查为什么没梯度 |

### 6.8 与已有结论的关系（**先验不利，必须写明**）

**这是第 4 次「让模型看见需求」，前 3 次都没测出：**

| 尝试 | 结果 |
|---|---|
| `mode: mixed → demand_edge` | **−0.0518**，t=−3.207（显著**更差**） |
| `v1_on_pending_path` | +0.008，t=0.890（测不出） |
| `v2_bottleneck` | +0.0154，t=1.724（测不出） |

**性质不同**：前 3 次是**加一列**或**改模式**，这次是**加一条可学习的通路**。
但先验确实不站在这边。

**★ 附加价值**：本实验**同时回答另一个问题**——「**actor 现在还能不能学新东西**」。
若新通路梯度恒零，说明训练真饱和了，**所有增益类旋钮都该停**，方向要转向别处。
这比单看成功率差更有信息量。

### 6.9 实现时必须处理的坑

**坑 1：需求边在 `edge_index` 里是加倍**

```python
# graph_builder.py:404-405 docstring
"""physical edges first as (src,dst) then (dst,src),
   followed by the same doubled representation for demand pairs."""
```

⟹ `edge_index` 里需求部分有 `2*n_demand` 行，而 `demand_emb` 只有 `n_demand` 行。
**下标对不上**，直接写会静默取错行（不报错，结果错）。
映射：`edge_index` 里第 `2*n_phys + 2*i(+1)` 行 ⟹ `demand_emb[i]`。

**坑 2：零候选必须输出 0**

`α` 在空集上做 softmax 会得 NaN。必须显式处理，且造反证要覆盖。

**坑 3：BFS 索引每步重建**

需求边和物理边**每步都在变**（实测 1~12 条 / 134~205 条），
所以稀疏对 `(i, j)` 必须**每步重建**。用 numpy 建索引，切成 `index_add_` /
`segment_softmax` —— 与现有 `_node_message` 同一路子。

### 6.10 配置形式

```yaml
# configs/train_dminj.yaml
model:
  encoder:
    demand_injection: true          # 默认 false ⟹ 逐位等于现状
    demand_injection_hops: 8        # 与 relay_importance.max_path_links 一致
```

**默认 `false` 时走当前代码路径** ⟹ 所有历史臂行为逐位不变。

### 6.11 执行顺序

```
① 等第五部分跑完
② 实现（配置开关 + 零初始化 + 梯度记录）
③ 跑预检：
   · 差异字段恰好 1 个（demand_injection）
   · 维度不变（node_dim 17 / edge_dim 44）
   · BC 权重：missing 只含新键，dropped = 0
   · ★ 造反证：开关为 false 时前向**逐位等于**基线
   · ★ 新参数梯度在 1 轮训练后非零
④ 起 dminj_s{42..46}（5 条）
⑤ 收结果，与 u0ctl 配对
⑥ 更新本文件与记忆
```

⚠ 全部改动**必须在训练空档期做**——记忆 `chain-must-not-be-resynced`。

### 4.13 补种子（**2026-09-22 起，进行中**）

**目的**：把 `u4quad`（4× 剂量）与 `u0ctl` 从 n=5 补到 **n=14**，让 urgency 可判定。

**为什么现在能判定**：

```
现有：Δ=+0.0101, SD(Δ)=0.0135（n=5）
补到 n=14 ⟹ SE = 0.0135/√14 = 0.0036
         ⟹ t = 0.0101/0.0036 = **2.80**
         ⟹ 临界值 t(13, 0.05) = **2.160**
         ⟹ t > 临界 ⟹ **能判定**
```

**★ 两组都要补**：配对需要同种子。只补实验组 ⟹ 新种子的对照组空缺 ⟹ 不能配对。

| 波 | 臂 | 种子 | 状态 |
|---|---|---|---|
| A | `u0ctl` | s47–55（9 条） | **在跑**（20:41 UTC 起） |
| B | `u4quad` | s47–55（9 条） | 待波 A 完成 |

**成本**：18 条 = 2 波 × 1.5 h ≈ **3 小时**（内存 18×25=450 > 250 可用，必须分波）

**启动器**：`/tmp/launch_urgency_ext.sh A|B`（含内存门，本波需余量 ≥200 GiB）

**判据**（预注册，与主实验一致）：窗口 u25/u30 均值，逐种子配对，
n=14（df=13，t_crit=2.160）。

| 结果 | 阈值 |
|---|---|
| 有效 | Δ ≥ +0.020 且 ≥12/14 同向 |
| 有害 | Δ ≤ −0.020 且 ≥12/14 同向 |
| **仍测不出** | \|Δ\| < 0.020 ⟹ 写有界结论 `u4quad 影响 < 0.020，n=14 未测出` |

**结果（待填）**：

| 臂 | 种子 | u25 | u30 | 均值 |
|---|---|---|---|---|
| `u0ctl_s47` | 47 | | | |
| `u0ctl_s48` | 48 | | | |
| `u0ctl_s49` | 49 | | | |
| `u0ctl_s50` | 50 | | | |
| `u0ctl_s51` | 51 | | | |
| `u0ctl_s52` | 52 | | | |
| `u0ctl_s53` | 53 | | | |
| `u0ctl_s54` | 54 | | | |
| `u0ctl_s55` | 55 | | | |
| `u4quad_s47` | 47 | | | |
| `u4quad_s48` | 48 | | | |
| `u4quad_s49` | 49 | | | |
| `u4quad_s50` | 50 | | | |
| `u4quad_s51` | 51 | | | |
| `u4quad_s52` | 52 | | | |
| `u4quad_s53` | 53 | | | |
| `u4quad_s54` | 54 | | | |
| `u4quad_s55` | 55 | | | |

**配对比较（n=14，待填）**：

| 对比 | Δ | SD(Δ) | t | 同向 | t_crit | 结论 |
|---|---|---|---|---|---|---|
| u4quad vs u0ctl | | | | | 2.160 | |
