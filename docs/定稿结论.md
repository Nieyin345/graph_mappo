# 定稿结论

> `训练诊断记录.md`（**11728 行**证据）的**成品**。日志是原料，这份是结论。
>
> **规则**（写死了，别破）
> 1. 每条 = **值 + 依据 + 证据强度 + 可推翻条件**；「依据」列必须指向证据档案的小节。
> 2. 「强度」只有五档：`★ 过预注册` / `★ 实测` / `单种子` / `未测出` / `未测`。
> 3. 结论被推翻时**不删**，改写为 `~~旧~~ ⟹ 新（推翻依据）` —— 论文的「我们试过」节需要它。
> 4. **数字必须能脱离服务器复算**（§7）。表里每个数字都要能从 `metrics.jsonl` 重算。
>
> 写于 2026-09-20。配套：`定稿方案.md`（怎么设计定稿实验）、`训练诊断记录.md`（证据）。

---

## 0. 一句话状态

**训练侧已经饱和，但论文的核心假设 H₁ 至今没有被检验过** —— 因为 H₁ 点名的
那个对照（**单智能体 DRL**）**全仓库没有实现**（§3 已核实）。

⟹ **定稿的第一优先级不是选超参，是补上 §3。** 训练侧的活儿（§4）可以停了。

---

## 1. 主结果：H₁ 的状态

论文 H₁（`00_研究方向.md` §3）：GNN+MAPPO 的请求成功交付率**显著高于单智能体 DRL
与启发式**，证伪标准 = **95% CI 重叠**。

| H₁ 的对照项 | 值 | 状态 | 出处 |
|---|---|---|---|
| 专家 `path_score_greedy_phased` | **0.7082** | ✓ 已冻结，节点无关 | `测试规范.md` §3 |
| `greedy_relay` | 0.4430 | ✓ 已冻结 | `训练诊断记录.md` §0 事实表 |
| `greedy_rate`（最大速率优先） | 0.4411 | ✓ 已冻结 | 同上 |
| `greedy_relay_diffusion_v3` | 0.3307 | ✓ 已冻结 | 同上 |
| `greedy_demand` | 0.2279 | ✓ 已冻结 | 同上 |
| `greedy_matching` | 0.1416 | ✓ 已冻结 | 同上 |
| `random` | 0.1194 | ✓ 已冻结 | 同上 |
| `greedy_qkp`（水位阈值） | 0.0730 | ✓ 已冻结 | 同上 |
| **本方法（GNN+MAPPO）** | — | **已测**：与专家无显著差异，方向略负 | §1.1 |
| **单智能体 DRL（DQN/GAT-DRL）** | — | ✗ **不存在** | §3 |

### 1.1 当前能说的最强的话（**不能更强**）

> 本方法与**专家**在留出协议上**无显著差异**，方向略负。

两次独立测量，同号同量级：

| 测量 | 代码路径 | Δ vs 专家（逐请求种子配对） | t | 逐种子胜/15 |
|---|---|---|---|---|
| 干净对照 `ent01_rerun_s{42,43,44}` | `metrics.jsonl` 的 `eval_validation` | **−0.0087** | −1.84 | — |
| 同三臂独立复现 | `run_baselines.py` + checkpoint 重评测 | **−0.0137** | — | 5 / 3 / 7 |

（出处：`训练诊断记录.md` §「九臂全表重标定」§四 + §「独立复现：RL vs 专家的成功率」。
逐种子胜数 5/3/7 都在 15 的一半附近 ⟹ **没有一条臂可靠地赢**。）

```
⚠ 已被推翻的旧说法（两层，都要记住）：
   ~~「RL 只比专家高 0.86 点」~~   那个 +0.0086 用的是**跨节点搬来的**臂
                                     ⟹ 硬件偏置不相消，见 §2.0。
   ~~「RL 比专家高约 2 点」~~       更早的文档说法，同因作废。
```

**⟹ 论文现在写不了「我们比专家好」，也写不了「我们比单智能体 DRL 好」**（后者根本没对照）。

★ **但这条负结果本身是可发表的**：它说明**专家基线不是软柿子**
（`path_score_greedy_phased` 用的是路径感知打分，见 `启发式与强化学习算法说明.md`），
而本方法在**密钥效率**上仍有一个大且稳的副产品（§1.2）。

### 1.2 唯一强成立的正面结果：密钥效率（**但它是副产品，不是能力**）

| 量 | 值 | 出处 |
|---|---|---|
| RL 对专家**省密钥** | **34.4%**（三训练种子 34.9 / 37.4 / 30.8） | §「密钥效率」§10703 |
| 逐请求种子配对 | **45/45 全同向**，逐臂 t = **−6.09 / −6.50 / −6.76** | 同上 |

⟹ **这是本项目信噪比最高的一条读数**（|t|>6）。两个独立测量、不同权重、
不同代码路径给同一个数（34.4% vs 记忆中的 36.5%）。

⚠⚠ **但它不能写成「能力」**：生效奖励里**只有 `served`(101.4%) 与 `failed`(1.7%) 非零**，
**奖励对生成量完全无感** ⟹ 省密钥是**无人看守的零空间漂移**，不是策略学到的目标。
正因无人看守，它**也可能漂回去**。论文里必须这么写，否则一旦被问「你的奖励里有密钥项吗」
就站不住。

---

## 2. 可采纳的默认值

| 项 | 取值 | 依据 | 强度 | 可推翻条件 |
|---|---|---|---|---|
| `entropy_coef` | **0.01** | 0.001 ↔ 0.01：Δ=−0.0109 t=−1.26 ⟹ 测不出 | 未测出 | 加功效 |
| `gae_lambda` | **0.90** | n=5 Δ=**+0.0146** SD 0.0090 t=**+3.61**，df=4 临界 2.776 ⟹ 过线，5/5 同向 | ★ 过预注册 | 补种子后失显著 |
| `gamma` | **0.99** | 0.999 为 −0.0284（单种子，两个独立实例同向同量级） | 单种子 | ⚠ 未经重标定（§2.0） |
| `value_coef` | **0.5** | 剂量-反应**看 SD 不看 Δ**：SD 从 0.0045 涨到 0.0301（6.7 倍）⟹ 剂量有害 | ★ 剂量-反应 | 见 §2.2 |
| `actor_lr` | **0.0003** | 两侧扫描**都测不出**（1e-3：Δ=−0.0046 t=−0.44；1e-4：Δ=−0.0050 t=−0.64）⟹ **没有证据要求改** | 未测出 | 加功效 |
| `minibatch_size` | **256** | 512 学习上测不出（t=+2.05），**内存上纯亏**（PSS +17.5% ⟹ 并发 9→8） | ★ 实测 | — |
| `clip_eps` | **0.1**（现用值） | 0.2 在**诊断场景**低于 base（−0.0023） | 单种子（诊断场景，§4.1） | — |
| `epochs` | **1** | >1 一律有害且越多越差（**诊断场景**，单调） | 单种子（诊断场景，§4.1） | — |
| `batch_chunk` | **256** | yaml 现值 64 每 run 多 5.08 GB（+44%），无学习代价 | ★ 实测 | — |
| `OMP_NUM_THREADS` | **固定值** | 线程数**确定性改变结果**（差 0.018）⟹ A/B 必须固定 | ★ 实测 | — |
| `seq_len` | 保持 240 | `hist32` 未收口（§5） | 未测 | — |

★ **注意这张表**：全项目至今**只有一个方向**通过了预注册判据（`gae_lambda` 0.90），
**其余每一条都是「测不出」**。「训练侧已经饱和」这句话的量化形式就是这张表。

★★ **`actor_lr: 0.0003` 是被改正过的** —— 先前的记录说「0.001 是唯一会掉的方向
（−0.0104 对 BC）、0.0003 是峰」；换成干净对照后**整体作废**：两侧都是 −0.005、|t|<0.7。
根因就是 §2.0 的节点偏置（`alr1e3` 的旧效应量 −0.0242 里 **81% 是偏置**）。
**⟹ 论文不许写「我们扫出了最优学习率」**，只能写「两侧都测不出差异」。

### 2.0 ★★ 节点偏置 0.0197 —— 读任何数字之前先过这一关

**同一个配置换一台机器，平台窗口差 0.0197。** 直接测出来的（两个对照同配置、
同种子、同 BC 权重、8 线程，`resolved_config` 逐字段只差 `run_name`/`num_updates`）：

| 对照 | Δ vs 专家 | 跑在哪 |
|---|---|---|
| `ent01_t8_s*` | **+0.0109** | 旧节点 |
| `ent01_rerun_s*` | **−0.0087** | 本节点 |
| **之差** | **+0.0197** | = 节点偏置 |

参照量级：**单种子分辨率 ~0.035、4↔8 线程差 0.018** ⟹ **偏置与它们同量级**。

**这条改写了全库判读**：旧效应量 ≈ 节点偏置 + 一个小的残差。
`alr1e3` 的 −0.0242 = −0.0197 + (−0.0046)；`ent03`/`w329p` 的残差**符号翻正**
⟹ 那两个「效应」**纯是偏置**。连「早期效应」也是：`gae99` u10 的
t 从 **−43.0 塌到 −2.9**，因为偏置自己的时间剖面在 u5/u10 最大。

**⟹ 三条硬规矩（做任何新判读前先过）**

1. **臂不能跨节点搬**。跨节点对照给出的 +0.0109 是**假的**，干净对照是 −0.0087。
2. **u1 指纹是入场券**。`u1` 的 rollout 在第一次梯度更新**之前**，只由
   「BC 权重 + 种子 + 线程 + 代码 + 硬件」定，**与臂自己的超参无关**。
   同种子两侧必须**逐位相同**；不同 ⟹ 中间有未记账变量，后面的判读一律不可信。
   （九臂全表里它精确地在「改优化器」与「改 rollout」两类臂之间分开，
   而唯一不符的正是跨节点那条 `ent01_t8`：0.857759 vs 0.872510。）
3. **2026-09-19 之前的单种子读数都可能被污染**，除非 u1 指纹证明同节点。
   ⟹ 这也是 §2 表里 `gamma` 标「未经重标定」的原因。

⚠ **这条不是理论风险，是已经咬到过人的**：我正在写这份文档时，就把
`actor_lr: 0.0003` 按旧记录标成了「★ 实测」，直到翻到证据档案里那节写着
**「上面这一节（actor_lr）与 gae99 的效应量全部作废」**。**顶部结论被底部推翻**，
这个仓库是这么长的 —— **引用任何一条读数前，先 grep 有没有反转节**。

### 2.1 表里最容易被误读的一行：`λ=0.90` 是「过线」不是「+0.0146 是真效应」

判定脚本**自己**写了这条限制，照抄进论文前必须一起抄：

> 过线只说明「**比 0 大**」，不等于 +0.0146 是真效应 —— 越线靠的是**功效不是效应量**。
> 且 wave5 的 n=3 读数 +0.0167 与本次**共用 s42/43/44 三个种子** ⟹ **不是独立复现**。

---

## 3. ★ H₁ 的缺口：单智能体 DRL 基线（**挡论文，第一优先级**）

### 3.1 事实核实（2026-09-20，我自己跑的，不是推断）

```
grep -rniE "single_agent|single-agent|\bdqn\b" qkd_rl/ scripts/ configs/ tests/
⟹ 零命中
```
（`.claude/skills/` 下的命中不算 —— 那是外部技能库，不是本仓库实现。）

**⟹ `定稿方案.md` §0 的断言成立：H₁ 点名的对照在仓库里不存在。**

### 3.2 切口勘察（结构勘察 + **两条被推翻的直觉**）

**「协调方式」这个变量，代码里确实切得开**：

| 项 | 位置 | 含义 |
|---|---|---|
| **协调机制** | `qkd_rl/env/action_resolver.py:130` `_resolve_mutual_choice` | `u->v` 生效 **iff** `u` 的 Tx 指向 `v` **且** `v` 的 Rx 接受 `u`（**互选**） |
| **开关** | `configs/env_full.yaml:21` `action_resolver.mode` | 四值：`mutual_choice`（**现用**）/ `priority_matching` / `greedy_rate_matching` / `max_weight_matching` |
| **实际采样** | `qkd_rl/rl/algos/policy.py:107` `_sample_matching` | **顺序**（不是一次独立 argmax）的在**可行弧 + STOP** 上采样，掩码同时表达 Tx-out≤1 / Rx-in≤1 / 对端不同 |
| **critic 作用域** | `qkd_rl/rl/models/graph_mappo.py:690` `GlobalCritic` | 全局池化（`typed_mean`），`gae.py` 每步**一个标量**，全体 agent 共享同一 advantage |
| **暖启动** | `qkd_rl/rl/algos/mappo_trainer.py:1410` | **已有** `_upgrade_state_dict_for_model`，widened/dropped/missing 全**大声打印** |

#### ★★ 推翻之一：`mutual_choice` **不是**「学出来的协调」

我原以为互选是策略学出来的。**不是**：它**在初始化时就冻结了**。
策略提交 `(tx, rx)`，但**哪个 (tx,rx) 组合会被接受，完全由环境的规则决定**。
actor 只学「报哪个邻居」，**互选协议本身从没被优化过**，永远静止。

⟹ 所以「把协调从策略里摘掉」在 `mutual_choice` 下**没有对应的改动**。

#### ★★ 推翻之二：「每节点独立选」+ `mutual_choice` = **一个无用的策略**

`qkd_rl/baselines/_matching.py:1-12` 的 docstring 是**项目自己早就试过的记录**：

> 「per-node greedy baselines 以前**各节点独立**选自己最好的链路。
> 在 `mutual_choice` 下**几乎从不产生被激活的边**（两端必须互选），
> **所以那些策略实际无用**。……改成全局贪婪匹配，并提交**互选**动作。」

**这是直接的实测证据**：独立选择 + 互选 ⟹ 退化成 `greedy_matching` = **0.1416**
（§1 表里最差的一档）⟹ 那不是「协调变弱」，是**根本不工作**。

⚠ 由此还能推出一步：因为 `stop_logit` 是**可学习标量**而互选是**冻结规则**，
「不协调」真发生时（两个 agent 互抱死锁），**任何学习都解不开**。
这正是 `env.py:169-174` 那条 `RuntimeError` 在保护的东西。

#### ⟹ 正确的对照形态（**必须换 `priority_matching`**）

「去协调」臂必须让**环境替策略裁决**，而不是让互选规则拒绝它：

| 臂 | 改法 | 变量 | 风险 |
|---|---|---|---|
| **A. 去图** | `model.encoder.num_layers: 0` | 有无消息传递 | 低（见下 §3.3-a） |
| **B′. 去协调** | `action_resolver.mode: priority_matching` | 策略提方案 vs 环境裁决 | 中（见 §3.3-b） |
| **C. 真·单智能体** | 单个全局 actor（非每节点独立） | **最贴合 H₁ 字面** | 高（需写代码） |

**A + B′ 同时开 = 最接近「单智能体 DRL」**：无消息传递、
每节点只按自身 logit 出弧、靠优先级匹配去冲突。
**但论文里必须正名为「独立 actor + 全局 critic」**，不能叫「单智能体 DRL」——
除非连 critic 也去中心化（改动面大得多，见 §3.3-c）。

### 3.3 ⚠ 四条硬约束（**动手前必须逐条过，否则对照不成立**）

**a. 保住 BC 起点 ⟹ `edge_scorer` 的输入维必须保持 `hidden*3`。**
`SharedNodeActor` 的弧分永远看**两个端点**（`graph_mappo.py:638`
`pair_emb = cat([node_emb[src], node_emb[dst], edge_emb[pos]])`），
且 `tests/test_model_forward.py:43` 有硬断言 `edge_scorer[0].in_features == hidden * 3`。
若把输入缩到 `hidden*2`，形状**变窄** ⟹ `_upgrade_state_dict_for_model` 的
`_expand_last_dim` **只处理变宽** ⟹ 走 `dropped` 分支 ⟹ **BC 的 actor 权重全丢**，
「唯一变量」就不唯一了。
⟹ **做法**：`num_layers: 0` 时把 `node_emb[dst]` 位填零（保留 `hidden*3` 形状），
**不要**改输入维。注意 `graph_mappo.py:638` 与 `:1029` 是同一段的**两份拷贝**，改一处必改两处。

**b. 必须换 resolver，否则 `env.py` 会抛错。**
`env.py:169-174` 校验「策略采样的匹配 == resolver 执行的匹配」，
`mutual_choice` 下不一致就 `RuntimeError`。独立出弧**必然**不一致 ⟹
**必须** `priority_matching`（trainer 白名单已含它，`mappo_trainer.py:293-304`）
或传 `expected_matched_edges=None`。

**c. PPO 的 log-prob 口径必须同步换。**
现状是**全节点共享一个 per-decision mean 的联合 log-prob**
（`policy.py:254-272` `_fill_node_tensors` + `mappo_trainer.py:1029-1057`）。
改成独立策略后要换成**每节点自身 log-prob**。
⚠ 注释 `mappo_trainer.py:1036-1044` 明确记着：**用 sum 会让
`clip_eps=0.1 / target_kl=0.02` 一次更新就被突破** ⟹ 口径换了要重新标定这两个值。

**d. critic 作用域是关键决策点（决定改动面大小）。**
去中心化 critic 要同时动 **3 处**：`value_head` 输出维、
`RolloutStep.value` 形状、`compute_gae` 的 per-node TD。
只去中心化 actor 而保留全局 critic ⟹ 切口就只是 (a)(b)(c)，
是**「只改协调方式」的干净对照**。

**e. 环境规则一旦改动，u1 指纹必须变。**
`B′` 把 resolver 从 `mutual_choice` 换成 `priority_matching` ⟹ **rollout 不同** ⟹
**u1 指纹与对照臂不再逐位相同，这是正确的、预期的**。
（参见九臂全表：`mode_de` 改环境、`w329p` 改窗口，指纹都变了 —— 指纹正是靠这个区分「改优化器」和「改 rollout」。）
⟹ **别把指纹不同当故障**；但**必须**记录它变了，作为「部署确实生效」的证明。

### 3.4 下一步（**等节点回来再做，别盲写**）

项目有条硬规矩：**没跑过的代码路径藏着 bug**（记忆 `never-run-code-path-hides-bugs`）。
节点不可达时写的基线实现，等节点回来还要返工，反而吃掉节点仅剩的时间。

⟹ 节点恢复后按此序：
1. **先跑 `A`（`num_layers: 0`）** —— 纯配置、零代码风险、立刻可跑。
   它单独就能回答「图结构值多少」，是**最便宜的结构性消融**。
2. **再跑 `B′`（`priority_matching`）** —— 纯配置，但**必须与 A 的 resolver 设定一致**才可比。
3. **看 A+B′ 是否已能构成 H₁ 需要的对照**；不够再写 `C`。
4. ⚠ **先读 `tests/test_model_forward.py:43` 与 `test_config_contract.py` 两条契约**，
   再决定动不动模型结构 —— `ConfigValidator._validate_options`（`core/config.py:161`）
   的设计意图是「YAML 里有但没实现的值一律抛错」，**新增开关必须同时加 validator 分支**。

---

## 4. 明确关闭的线（连「试过」一起写）

论文的消融节要的正是「试过什么、为什么没用」。**负结果是消融表，不是废料。**

| 线 | 改动 | 读数（**重标定后**） | 判定 | 出处 |
|---|---|---|---|---|
| `gae_lambda` | 0.95 → **0.99** | −0.0572，t=−1.57（9 条里唯一有大残差的） | 测不出 | §「九臂全表重标定」 |
| `gamma` | 0.99 → **0.999** | −0.0284（单种子，两个独立实例同向同量级） | 关闭 | §8600 前后（⚠**未重标定**） |
| `entropy_coef` | 0.01 → **0.001** | Δ=−0.0109，t=−1.26 | **正式关闭** | `server_results/ent001_verdict_2026-09-20.txt` |
| `entropy_coef` | 0.01 → **0.03** | +0.0066，t=+0.60（**残差符号已翻**） | 测不出 | 同上 |
| `actor_lr` | 0.0003 → **1e-3 / 1e-4** | −0.0046 (t=−0.44) / −0.0050 (t=−0.64) | **整节作废**（旧判定「1e-3 过线」是偏置，§2.0） | §「重标定终判」 |
| `value_coef` | 0.5 → 0.75 → **1.0** | Δ 非单调；**SD 0.0045 → 0.0301** | 剂量-反应**看 SD 不看 Δ** | §「vcoef 剂量-反应」 |
| `minibatch_size` | 256 → **512** | 学习 +0.0066 t=+2.53 测不出；内存 PSS +17.5% | **内存否决**（见 §4.1） | §9689 + §10602 |
| `epochs` | 1 → **2** | −0.0309，t=−1.09 | 测不出（诊断场景里「>1 一律有害」见 §4.1） | §「九臂全表重标定」 |
| `model.mode` | mixed → **demand_edge**（结构） | −0.0145，t=−0.78 | 测不出（**这是结构性消融**） | 同上 |
| 窗口 | → **0–329**（`w329p`） | +0.0029，t=+0.24（**符号已翻**） | 测不出 | 同上 |
| 线程数 | 4 / 8 | 差 0.018 | **不是旋钮，是实验条件** | `测试规范.md` §6 |
| `lr` 退火 | 后期收小步长 | 全库 46 臂 u25→u30 = **跌 24 / 涨 22（52%）**，均值 −0.0007 | **证伪** ⟹ 省 3 个 run | §「lr 退火动机」 |
| `batch_chunk` 调参 | 64 → 256 | 仅内存收益，无学习收益 | 只采纳配置（§2），不立项 | §「batch_chunk 是内存旋钮」 |
| `holdout` 暖启动 | — | — | 关闭（未做） | `定稿方案.md` §6.2 |

### ★ 4.1 一处**两 regime 矛盾**，必须如实写（不能只写「更差」）

`minibatch 512` 与 `value_coef 1.0` 在**诊断场景**（240 步、固定第 0 天）里
**是仅有的两个越过专家线的变体**（0.8226 / 0.8221，各 +0.0397）。

但搬到**真实配置**后（重标定）：`mini512` 学习上测不出（t=+2.53）且内存 +17.5%；
`value_coef 1.0` 的 SD 恶化 6.7 倍。`epochs 2` 也一样（诊断场景有害、真实配置 t=−1.09 测不出）。

⟹ **诊断场景的排名不迁移**（记录自己就写了「真实预设和诊断预设不是一回事」）。
论文写消融时**必须两个 regime 都列**，否则读者会以为我们没试过。

### ★ 4.2 一条方法错误：**用「绝对水平 vs 外部锚点」当判据是循环论证**

记录里我自己犯过并记下来了 —— 因为它很容易重犯：

> 我曾写「专家锚**独立证实**了配对 Δ」，理由是 `alr1e3 − 专家 = −0.0133`、
> `对照 − 专家 = +0.0133`，两者差 0.0266 ≈ 配对 Δ 0.0242。
>
> **这是循环的**：专家是**本节点**测的，而那个对照是**旧节点**跑的。
> 「对照 − 专家 = +0.0133」里**混着跨节点偏置**。
> 用干净对照重算：`ent01_rerun − 专家 = −0.0087`（**对照自己也在专家之下**），
> 于是 `alr1e3 − 对照 = −0.0133 − (−0.0087) = −0.0046` ⟹ **效应消失**。

**判据**：拿「绝对水平 vs 外部锚点」当证据时，**必须先确认对照组相对该锚点也是干净的**。
否则锚点只是把**同一个未记账变量**再算了一遍，看起来像独立证据。
（好在**专家锚本身节点无关** —— 它是纯启发式、无 matmul ——
所以偏置全在 RL 那一侧，不抵消。）

### 4.3 被否决的方案（记了原因，防止重新捡起来）

| 方案 | 为什么否 |
|---|---|
| 降 `critic_lr` | 记忆 `actor-is-frozen-by-advantage-collapse`：是**收敛不是 bug** |
| 改 GAE 终止语义 | 我改成 `truncated` 反而 **−0.0399**，已回滚 |
| 关掉优势归一化 | 照样反号，且把 kl 压小 **88 倍**（加重「actor 不动」） |
| 「杀最晚启动的」看门狗 | 在错误阈值下**精确杀掉了唯一想保的对照臂** |
| `r4` 真实配置筛选 | 跑在**播种修复之前**、每臂 1 种子，outputs 已丢 ⟹ 无效 |
| `malloc_trim` 提高并发 | trim 当即砍 37.7%，**但下一轮长回同一高水位** ⟹ 上限不变 |

---

## 5. 待收口（只有一条）

| 项 | 现状 | 为什么值得等 |
|---|---|---|
| **`hist32`**（时序编码器） | `hist32_s43` u28 ｜ `hist32_s44` u19 ｜ `hist32v3_s42` u17 ｜ 对照 `ent01_rerun_s{42,43,44}` 已齐 u30 ｜ 判定 `/tmp/hist_verdict.txt` **未产出** | **唯一一项结构性改动的消融**（不只是超参），论文消融节要它 |
| `gae85`（λ 0.90→0.85） | 链**随节点归零**（从未真正跑起来） | 纯调参，收益上限 ~0.015，**不影响 H₁** |
| `u50` 续跑（u30 是否截断） | **随节点归零** | 判断「u30 是不是没跑够」，影响 §2 所有平台窗口读数 |

⚠ **节点 `clnode316` 于 2026-09-20 21:16 UTC 失联**，上述在跑的臂**全部归零**，
且 `/tmp` 下的判读脚本一并丢失。详见 §6。

---

## 6. 证据缺口（**下次抓结果时先补这个**）

用户的要求：**「结果抓到本地，不要权重，只要好的结果，以及如何调的参数、如何优化的结构」**。
现状：

| 位置 | 内容 | 够不够 |
|---|---|---|
| `.tmp/results/` | 11 个文件：gae90/ent01 n=5 的 `metrics.jsonl` + verdict + 1 份 `resolved_config.yaml` | 够复算 §2 那条 |
| `server_results/gae90_n5_raw/` | 上表 12 个文件的备份 | ✓ |
| `server_results/`（其余） | **2026-09-19 01:55 的旧快照** | ✗ **缺 gae90/hist32 等全部有效臂** |

⟹ 节点恢复后**立刻**跑 `bash .tmp/fetch_all.sh`。**只要 `metrics.jsonl` +
`resolved_config.yaml`**（`.pt` 权重不抓 —— 用户已明确）。

---

## 7. 论文数字必须能**脱离服务器**复算

**已实测通过**（2026-09-20，节点恰好不可达时顺手验的）：`.tmp/recompute_gae90_n5.py`
（**只标准库**）从 `.tmp/results/` 的 10 个原始 `metrics.jsonl` 重算，**逐数字复现**：

| 量 | 重算值 | 原判定文件 |
|---|---|---|
| u1 指纹 5/5 逐位相同 | ✓ | ✓ |
| Δ | **+0.0146** | +0.0146 |
| SD / SE | 0.0090 / 0.0040 | 同 |
| **t** | **+3.61** | +3.61 |
| 逐种子 Δ | `+0.0128 +0.0302 +0.0071 +0.0110 +0.0117` | 同 |

⟹ 结论**不依赖服务器上那个脚本是否正确**（换机器、换实现、从原始文件重算）。

### 复算脚本必须焊死的三条（每条都对应一次实测事故）

1. **eval 轮号按文件顺序交错读**，不按位置推 `seq*5`
   —— `eval_validation` 行**没有 `update` 键**；续跑把计数器归零但**追加**同一文件
   ⟹ 按位置推会**整体错位**。
2. **未知 df 一律抛错**，不许 `.get(df, 2.0)` 式静默宽松回退
   —— 曾有判读脚本拿 **df=14** 的 2.145 去判 **df=1** 的 t（真值 12.706，偏松 6 倍），
   若 |t| 落在 2.145~12.706 之间就会**宣布假显著**。
3. **逐训练种子配对 + 臂不能跨节点搬**
   —— 不配对时 15 种子 SE≈0.062，配对后 ≈0.012（**差 5 倍**）；
   **专家锚是纯启发式、节点无关**，所以它的偏置**不抵消**。

### 临界值表（唯一来源：`scripts/diag/stats_crit.py`）

| n（训练种子） | df | 临界值 |
|---|---|---|
| 2 | 1 | 12.706 |
| 3 | 2 | **4.303** |
| 4 | 3 | 3.182 |
| 5 | 4 | **2.776** |
| 15 | 14 | **2.145** |

⚠ **n=1 时 df=0，任何 t 都无限大** ⟹ **单种子决议在这套框架里没有合法判据**。
这正是「线程数」那条卡在「不是旋钮，是实验条件」的原因。

---

## 8. 定稿的下一步（按「挡不挡论文」排）

| # | 事 | 挡论文? | 依赖服务器? |
|---|---|---|---|
| 1 | **补单智能体 DRL 对照**（§3，先跑纯配置臂 A/B） | ★ **挡** | 是 |
| 2 | 按验证协议跑主对比表（15 请求种子，配对） | ★ **挡** | 是 |
| 3 | **先写死**「定稿用哪个 checkpoint」= `checkpoint_best_val.pt` | ★ **挡** | 否 |
| 4 | 抓结果到本地（§6） | 是 | 是 |
| 5 | `hist32` 收口（§5） | 否（消融节要） | 是 |
| 6 | `gae85` / `u50` | 否 | 是 |
| — | 再找新旋钮 | **明确不要做**（§4 已关闭 11 条线） | — |

⚠ **第 3 条要提前写死**，不能事后挑最好的一轮 —— 记忆 `selection-bias-max-of-k`：
`best_val` 的选择偏差是真的，只有用**不相交种子**才能估出来。

---

## 9. 这份文档怎么更新

- **只追加结论，不追加过程**（过程进 `训练诊断记录.md`）。
- 每条必须带**证据强度**（五档，见文件头规则 2）。
- 被推翻**不删**，改成 `~~旧~~ ⟹ 新（推翻依据）`。
- 每个数字必须能由 §7 的规则从 `metrics.jsonl` 重算；**重算不出来的一律不许进表**。
