# Four-step integration test; never use this preset for a scientific comparison.
env:
  episode_steps: 4
train:
  num_updates: 1
  rollout_steps: 4
  episodes_per_update: 1
  n_rollout_workers: 1
  ppo:
    minibatch_size: 4
  logging:
    eval_interval: 0
    checkpoint_interval: 1
validation:
  episodes: 1
  episode_steps: 4
